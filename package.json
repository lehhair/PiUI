{
  "name": "piui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi coding agent client — OCUI visual shell + Pi-native runtime",
  "license": "GPL-3.0-only",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "clean": "node scripts/clean.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:server": "npm run dev -w @piui/server",
    "dev:server:pi": "node scripts/dev-server-pi.mjs",
    "dev:host": "npm run dev -w @piui/server",
    "dev:app": "npm run dev -w @piui/app",
    "build": "npm run build -w @piui/protocol && npm run build -w @piui/pi-worker && npm run build -w @piui/server",
    "test": "node scripts/test-mock.mjs",
    "typecheck": "npm run build -w @piui/protocol && npm run build -w @piui/pi-worker && npm run typecheck -w @piui/server"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "patch-package": "8.0.1"
  }
}
