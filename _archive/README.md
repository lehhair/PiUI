# 归档

## opencodeui-baseline

原 OpenCodeUI `dev` 分支完整快照。

- **用途**：UI 壳、主题、布局、组件美术参考
- **不要**：继续在这里接 Pi 或当主开发树
- **可搬**：`src/components/*` 视觉组件、主题 CSS、Tauri 窗口装饰等
- **不要搬**：`src/api/*`、`@opencode-ai/sdk`、OpenCode 消息模型

主工程在仓库根目录 `packages/`。
